<div class="sidebar bg-light shadow-lg p-3">
    <h4 class="text-primary font-weight-bold">Quick Links</h4>
    <ul class="nav flex-column">
        <li class="nav-item">
            <a class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Home" ? "active" : "")" href="/Home/Index">Home</a>
        </li>
        <li class="nav-item">
            <a class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Task" ? "active" : "")" href="/Task/Index">Tasks</a>
        </li>
        <li class="nav-item">
            <a class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Categories" ? "active" : "")" href="/Categories/Index">Categories</a>
        </li>        
    </ul>
</div>

<style>
    .sidebar {
        padding: 15px;
        background-color: #3c4754; /* Light gray background */
        border-right: 1px solid #ddd; /* Subtle border */
    }

    .nav-link {
        padding: 10px 15px;
        color: #0077cc; /* Default link color */
        border-left: 4px solid transparent; /* No border by default */
        transition: border-color 0.3s ease, background-color 0.3s ease; /* Smooth transitions */
    }

        .nav-link:hover {
            background-color: #e9ecef; /* Light background on hover */
            text-decoration: none;
            color: #0056b3; /* Slightly darker blue on hover */
        }

    .active-link {
        font-weight: bold;
        color: #0056b3; /* Active link color */
        border-left: 4px solid #ff9800; /* Colorful strip for active item */
        background-color: #eaf4fc; /* Subtle background for active link */
    }
</style>